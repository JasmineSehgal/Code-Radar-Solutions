#include <stdio.h>

int main() {
    int x, y;


    // Read two integers from the user
    scanf("%d %d", &x, &y);

    // Calculate and print the sum
    printf("Sum: %d\n", x + y);

    return 0;
}